{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "RAILPACK",
        "buildCommand": "composer install --no-dev --optimize-autoloader && php artisan event:cache && php artisan route:cache && php artisan view:cache && php artisan storage:link && npm ci && npm run build"
    },
    "deploy": {
        "startCommand": "chmod +x start.sh && ./start.sh",
        "healthcheckPath": "/",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}

